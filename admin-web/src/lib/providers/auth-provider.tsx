'use client'

/**
 * Session Provider
 * This is a compatibility wrapper for the AuthProvider
 * It provides the same interface as auth.js session providers
 */
export { AuthProvider as SessionProvider } from '@/lib/auth/client'

