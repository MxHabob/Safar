apiVersion: 1

# Dashboard provisioning is disabled until dashboard JSON files are added
# To enable, add a provider below and add dashboard JSON files to
# the /etc/grafana/provisioning/dashboards directory
providers: []

